const { Then } = require('cucumber');
const checkAccessibility = require('../utils/browser/commands/check/checkAccessibility');

Then('I expect the {string} page has no accessibility errors', async function(selector){
  
  checkAccessibilitysdcjksndjk
});